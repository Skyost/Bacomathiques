comments:
  allowedFields: ["lesson", "level", "client", "author", "message"]
  branch: "master"
  commitMessage: "Nouveau commentaire par {fields.author} <Staticman>\n\n{fields.message}\n\n#comment-{@id}"
  filename: "{@timestamp}_{@id}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
  moderation: true
  name: "Bacomathiques"
  path: "_data/comments/"
  requiredFields: ["lesson", "level", "client", "author", "message"]
 